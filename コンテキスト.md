# わくわく学習アプリ 開発コンテキスト（引き継ぎ用）
**最終更新日時**: 2026-08-30  
**現在のバージョン**: v29

---

## 📌 1. 今回（v29）実装・修正した内容まとめ

- **🌸 バージョン管理・表示バッジの一元同期化**:
  - **1. 画面バージョンバッジの不整合解消**:
    - アカウント画面およびポータル画面の `<span class="cute-version-badge">` が「ばーじょん27」のままハードコードされていた問題を解消し、「🌸 ばーじょん29 🌸」に統一。
  - **2. バッジ表示の一元同期機構の導入**:
    - `app.js` の先頭にバージョン定数 `APP_VERSION_LABEL = '🌸 ばーじょん29 🌸'` を定義。
    - `DOMContentLoaded` 時に `initVersionBadges()` で画面上の全 `.cute-version-badge` へ一括反映する仕組みを導入。
    - 今後は `app.js` の定数を1箇所変更するだけで画面上の全バッジに自動反映され、更新漏れを構造的に防止。
  - **3. 全体バージョン・キャッシュの完全同期**:
    - `sw.js`: `CACHE_NAME = 'wakuwaku-learning-v29'`
    - `index.html`: 各種スクリプトのクエリを `?v=20260830_v29` に更新
    - `SPEC_AND_KNOWLEDGE.md`, `コンテキスト.md`, `README.md` のバージョン表記・履歴を v29 に完全同期。
  - **4. README.md の履歴表・リリース手順改訂**:
    - 履歴表に v27, v28, v29 の経緯を追記（CACHE_NAME 進行の理由を明記）。
    - リリース手順を「CACHE_NAME / ?v= / 表示バッジ（app.js定数） / 履歴表」の4点チェックへ改定。

---

## 📌 2. 前バージョン（v28）実装・修正内容まとめ

- **🛡️ 安全性・プライバシー・データ保全の強化**:
  - **1. 手書き認識の外部送信説明・同意・OFF設定・フォールバック（指摘18・19）**:
    - 初回書き取り開始前に「線の情報のみをGoogleサービスへ送信しAI採点する（写真や名前は送りません）」説明モーダル（`#modal-writing-ai-consent`）を表示し同意状態を保持。
    - 保護者設定に「手書きドリルのAI自動採点をつかう（`#cfg-ai-grading`）」ON/OFFトグルを追加。
    - オフライン時、OFF設定時、API通信エラー時は不正解にせず「手動採点（○・×ボタン）」へ自動フォールバック。
    - 固定Mac文字列のUser-Agent偽装を削除し `navigator.userAgent` を使用。
  - **2. innerHTMLへのXSS対策 & サニタイズ（指摘20）**:
    - `escapeHtml(str)` によるアカウント名、本タイトル、履歴項目のエスケープ徹底。
    - `isValidHttpUrl(url)` による `javascript:` や不正スキームのリンク無効化。
    - `importDataBackup()` での厳格な型・スキーマ検証とサニタイズ。
  - **3. 保護者PIN変更のモーダル化（指摘21）**:
    - 平文 `prompt()` を廃止し、4桁テンキー＆ドット伏字（●●●●）UIによる3段階モーダル（現在PIN → 新規PIN → 確認PIN）へ移行。
  - **4. ウィッシュリスト購入達成の保護者PIN認証化（指摘11）**:
    - `completeWishlistItem` に保護者PIN入力を必須化し、子供の誤操作によるポイント減算を防止。
  - **5. 履歴トリムと保存容量対策（指摘12）**:
    - `pushPointHistory(acc, entry)` 共通関数によりポイント履歴を最新300件に自動トリム。
    - `trimStudyLog(studyLog)` により学習ログを直近13ヶ月分に自動トリム。
    - `saveAccounts` catch での容量不足エラー通知。
  - **6. 破損データの退避（指摘4）**:
    - `loadAccounts` で壊れた生データを `wakuwaku_accounts_broken_<timestamp>` に退避し、無言の初期化・上書き損失を防止。
  - **7. `lifetimeEarned` 0判定の修正**:
    - `typeof acc.lifetimeEarned === 'number'` 判定により、新規アカウントの 0pt が `acc.points` に誤フォールバックする問題を解消。

- **🏗️ 構造・保守性・アーキテクチャの改善**:
  - **1. 学年判定の一本化（指摘23）**:
    - `getGradeForAccount(acc)` を唯一の判定関数とし、未就学児は1年、卒業生は6年、生年未設定は5年（標準）として必ず 1〜6 の整数を返すよう丸め処理を実装。
    - 表示用ラベル関数 `getGradeDisplayLabel(acc)` を新設し、「0年生」「-1年生」などの不正表示を根絶。
  - **2. 使われていない苦手記録の削除（指摘24）**:
    - `handleWritingGrading` から読み出し処理の存在しない `weakQuestions.push` を削除。
  - **3. 上限計算・ポイント加算の共通関数化（指摘22）**:
    - `grantPoints(acc, requestedPoints)` を新設し、`claimLoginBonus`, `applyEarnedPoints`, `unlockBadge` の上限計算コードを集約。
  - **4. 書き取りキャンバスの再設計（指摘33）**:
    - 巨大 `ImageData`（約57MB）を全廃し、ストローク配列ベースの再描画アーキテクチャ（`WritingCanvas`）に刷新。
    - `resize` / `orientationchange` 監視を追加し、画面回転時に解像度再計算＆線データを失わず即時再描画。
    - 「全消し → 1画もどす」で消去前のストローク配列と描画を完全一致復元。
    - `pointerleave` 即時確定を廃止し `setPointerCapture` / `pointerup` で確定（線の分断を防止）。
  - **5. タイピング画面のキー入力横取り防止（指摘34）**:
    - `isAnyModalOpen()` ヘルパーにより、モーダル表示中はタイピングの物理キーボード入力をスキップ。
  - **6. Service Worker の更新運用明記（指摘29）**:
    - `sw.js`（`v28`）と `index.html`（`?v=20260826_v28`）のバージョンを同期し、`README.md` に更新手順を明記。
  - **7. ドキュメント・データコメント修正（指摘31）**:
    - `SPEC_AND_KNOWLEDGE.md` の実績ボーナス（2.0〜5.0pt）および `writing_data.js` の収録件数コメントを実態に修正。

---

## ⚠️ 開発・検証の絶対ルール（次回以降必須）
コードを変更した際は、報告の前に**必ず以下を実施**すること：
1. **構文チェック**:
   ```bash
   node --check app.js && node --check quiz_data.js && node --check math_data.js && node --check typing_data.js && node --check writing_data.js && node --check sw.js
   ```
2. **実機・ブラウザ動作確認**:
   ローカルサーバー経由でブラウザを開き、画面遷移・ボタン反応・コンソールエラーの有無を確認する。

- **⌨️ タイピング強化（学年別アシスト制御 ＆ ローマ字揺れ入力の完全網羅）**:
  - **学年別のローマ字アシスト切り替え**:
    - **小学1・2年生（または未就学児）**: 現状維持（ローマ字ガイド＆次キーの光るアシストあり）。
    - **小学3年生以降（3〜6年生・卒業生）**: **ローマ字アシスト非表示（実践特訓モード）**。打つべきアルファベットガイド（`romaji-next`, `romaji-remain`）を隠し、日本語の表記（漢字＋ふりがな）だけを見て頭の中でローマ字を組み立てて打つ実践仕様。
  - **ローマ字表記の揺れ・外来語・合成打ちの完全網羅**:
    - 「フェ」: **`fe`** / **`hue`** / **`hule`** / **`huxe`** / **`fule`** / **`fuxe`**
    - 「ファ/フィ/フォ」: **`fa/fi/fo`** / `hua/hui/huo` / `hula/huli/hulo` / `huxa/huxi/huxo`
    - 「ティ/ディ/デュ」: **`thi/dhi/dhu`** / `ti/di` / `teli/deli/delyu` / `texi/dexi/dexyu`
    - 「チェ/シェ/ジェ」: **`che/she/je`** / `tye/sye/zye` / `chile/sile/zile`
    - 「ウィ/ウェ/ウォ」: **`wi/we/who`** / `ui/ue` / `uli/ule/ulo`
    - 「っ（促音）」: 子音重ね ＋ 単独打ち（**`xtu`**, **`ltu`**, **`ltsu`**, **`xtsu`**）
    - 「し/ち/つ/ふ/じ」: `shi/si/ci`, `chi/ti`, `tsu/tu`, `fu/hu`, `ji/zi`
- **🌸 バージョン表示（🌸 ばーじょん20 🌸）**:
  - 各画面下部バッジを更新。

- **⏭️ 問題スキップ機能（バグ回避・ペナルティなし）**:
  - 全科目（漢字読みクイズ、漢字書き取り、算数クエスト、タイピング）の各問題画面に「⏭️ スキップ」ボタンを新設。
  - スキップ時は不正解記録・減点・苦手登録などのペナルティは一切発生せず、単純に次の問題（または単語）へスキップ。
- **🪙 ポイント残枠＆上限の可視化（日上限・月上限）**:
  - ポータル画面上部に **「📅 きょうの残枠（1日上限50pt）」** と **「🗓️ こん月の残枠（月間上限1000pt）」** のリアルタイム進捗バーバナーを設置。
  - 各科目カードに最新の配点（読み:0.5pt、書き:1.0pt、算数:1.0pt、タイピング:0.3〜0.8pt）を明記。
- **⚖️ 科目間ポイントウェイトの改定**:
  - ユーザー指定比率 **「漢字クイズ:漢字書きとり:算数:タイピング = 0.5 : 1 : 1 : 0.5」** に完全準拠。
    - 漢字読みクイズ: 1問 **0.5pt**（苦手克服時は+0.5pt）
    - 漢字書き取り: 1問 **1.0pt**
    - 算数クエスト: 1問 **1.0pt**
    - タイピング: コース基準 **0.5pt**（かんたん0.3〜0.4pt / ふつう0.5〜0.6pt / 激ムズ0.8pt）
- **🌸 バージョン表示（🌸 ばーじょん19 🌸）**:
  - 各画面下部バッジを更新。

- **✍️ iPad Apple Pencil / 手書き対応「漢字書き取りドリル」新設**:
  - **4科目グリッド化**: ポータル画面を 2×2 の4カード構成（漢字読み・漢字書き・算数・タイピング）に拡張。
  - **7:3 出題バランス比率**:
    - 現在の学年（例: 5年生）の配当漢字 **70%** ＋ 1学年下（4年生）の復習漢字 **30%** を自動抽出・シャッフル出題（1年生は100%）。
  - **十字リーダー線つき手書きキャンバス（`WritingCanvas`）**:
    - Pointer Events による Apple Pencil 筆圧感知・滑らかな鉛筆ストローク。
    - ツールバー: ✏️ えんぴつ、🔴 赤ペン、🧹 消しゴム、↩️ 1画もどす（Undo）、🗑️ 全消し。
  - **見本重ね合わせ ＆ 自己採点フロー**:
    - 「👁️ こたえ合わせ」ボタンで見本の楷書体が現れ、うすく重ねて見比べるトグル機能。
    - 💮 できた！（せいかい / +2.0pt） / 🌱 もういっかい（苦手記録）の自己採点。
  - **結果画面 & ポイント連携**: 獲得ポイントの通帳記録、満点・高得点の花火演出。
- **🌸 バージョン表示（🌸 ばーじょん18 🌸）**:
  - 各画面下部バッジを更新。

- **🦉 ポップアップ・キャラクターガイド（ホウホウ博士のあそびかたチュートリアル）**:
  - **オリジナル3Dキャラクター「ホウホウ博士」**: 案内役の可愛いフクロウ博士（`character_guide.png`）を生成・実装。
  - **インタラクティブ・スライド解説（全4ステップ）**:
    - ① アカウント選択とアバター・色カスタマイズ
    - ② 3科目（漢字・算数・タイピング）の遊び方と特徴
    - ③ ポイント獲得・ポイント通帳・ほしい本ウィッシュリスト
    - ④ 苦手問題の自動記憶と克服花火ボーナス
  - **🔊 音声読み上げ対応（Web Speech API）**: 「こえできく」ボタンで、文字が読めない低学年のお子様でもホウホウ博士の可愛い声でナレーションを聞くことが可能。
  - **アクセス性**: アカウント選択画面＆ポータル画面の双方に「🦉 あそびかたガイド」ボタンを配置。
- **🌸 バージョン表示（🌸 ばーじょん17 🌸）**:
  - アカウント選択画面＆ポータル画面下部バッジを更新。

- **📱 iPad / Safari ホーム画面追加対応 ＆ PWAフルスクリーン化**:
  - `manifest.json` と iOS専用メタタグ（`apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style` 等）を完全整備。
  - **専用アプリアイコン**: 高解像度・ポップで可愛い「学 / わくわく」アプリアイコン（`apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, `favicon.png`）を生成・配置。
  - **フルスクリーン起動**: iPadで「ホーム画面に追加」を行うと、Safariのアドレスバーやタブを非表示にし、本物のネイティブアプリのように全画面で起動可能（お子様の誤操作・離脱防止）。
  - **Service Worker（`sw.js`）**: オフラインキャッシュに対応し、電波が不安定な環境でも快適に起動・学習可能。
- **🌸 バージョン表示（🌸 ばーじょん16 🌸）**:
  - アカウント選択画面＆ポータル画面下部バッジを更新。

- **✨ 算数・タイピングの選択ボタンスタイル統一（漢字ドリル準拠）**:
  - 算数の「なん問チャレンジする？」およびタイピングの「むずかしさをえらぶ」ボタンに `.pop-btn` デザインを完全適用。
  - ホバー時の浮き上がりやアクティブ時のグローシャドウ演出を統一。
- **🦅 タイピング最上位コース名変更**:
  - 「たつじん」→「**激ムズ**」に表記をアップデート。
- **🪙 タイピングポイント制度の小数点対応 ＆ 難易度別傾斜インセンティブ**:
  - タイピングのポイントを小数点対応化（内部計算＆表示 `formatPoints`）。
  - **難易度×ランク別ポイント表**:
    - 🐣 **かんたん**: ランクS: 0.8pt / ランクA: 0.5pt / ランクB: 0.2pt
    - 🐥 **ふつう**: ランクS: 1.2pt / ランクA: 0.8pt / ランクB: 0.4pt
    - 🦅 **激ムズ**: ランクS: 1.8pt / ランクA: 1.4pt / ランクB: 0.8pt
  - 「激ムズ」で高ランクを取れば、かんたんの約3倍以上のポイント（最大1.8pt）が獲得可能になり、難しいコースに挑戦する強いインセンティブを付与。
- **🌸 バージョン表示（🌸 ばーじょん15 🌸）**:
  - アカウント選択画面＆ポータル画面下部バッジを更新。

- **🎉 キラキラ紙吹雪＆スター花火エフェクト（Canvasパーティクル）**:
  - 外部ライブラリ不使用の超軽量Canvasパーティクルエンジン `ConfettiFx` を実装。
  - **クイズ・算数・タイピングの結果発表時**: 満点・クリア時に画面いっぱいにカラフルな紙吹雪＆星（✨⭐🌸）が舞い上がる！
  - **苦手問題克服時**: 「こくふく！」達成時に祝福のミニ紙吹雪が発射。
- **🔊 Web Audio API による効果音（SE）システム ＆ 音声ON/OFF切り替え**:
  - 外部MP3ファイルを使わず、ブラウザ標準のWeb Audio APIで生成する超軽量シンセサウンド。
  - **正解音**: 「ピンポーン♪」（高音2和音・サイン波）
  - **不正解音**: 「ブブー」（短形波ビート）
  - **タイピング打鍵音**: 「ピッ」（爽快な高音タップ）
  - **結果発表ファンファーレ**: 「パラパラーン✨」（明るいアルペジオ）
  - **コイン・ポイント交換音**: 「チャリン🪙」（高周波ベル）
  - **ボタンタップ音**: 「ポコッ」
  - **画面右上固定の音声トグルボタン（🔊 / 🔇）**: ワンタップでミュート切替可能、状態はLocalStorageに自動保存。
- **🌸 バージョン表示（🌸 ばーじょん14 🌸）**:
  - アカウント選択画面＆ポータル画面下部に可愛いパステルピンクバッジで常時表示。

- **🎨 アカウント選択画面のテーマカラー自由選択（12色パレット）**:
  - 設定画面に12色のカラースウォッチ（いちご、みかん、ひまわり、みどり、そら、ぶどう、さくら、みずいろ、ゴールド、エメラルド、パープル、くろ）を追加
  - 選択中のカラーに ✓ マークとリングハイライトを表示
  - アカウントカードのアクセントカラー、アバター背景、バッジ等にリアルタイム反映
  - `accounts[i].themeColor` としてLocalStorageに保存
- **🖼️ アバターアイコン自由選択（12種絵文字グリッド ＆ 📸 カメラ・写真アバター）**:
  - **絵文字グリッド**: 12種類の動物・マーク絵文字（🐱🐶🐼🐰🦊🐻🦁🐨🐸🐧🦄⭐）からワンタップ選択
  - **カメラ / 写真ライブラリ対応**: 
    - `<input type="file" capture="user">` でiPad/iPhoneのカメラ撮影およびフォトライブラリ選択に対応
    - 選択された画像を **Canvas上で自動的に中央正方形クロップ ＆ 200×200px (JPEG 80%) に圧縮** してBase64保存（LocalStorageの容量制限を圧迫しない安全設計）
    - 「🗑 写真をけす」ボタンで写真削除・絵文字に戻す機能を追加
  - **全画面連動表示**:
    - アカウント選択カード（写真 > 絵文字 > 名前頭文字 の優先順で丸枠表示）
    - ポータルヘッダーの小アバター（写真 > 絵文字 > 名前頭文字 で表示）
    - 設定画面の丸アバター（リアルタイムプレビュー）
- **🛡️ アカウント誤削除防止の安全確認モーダル（YES / NO）**:
  - 「このアカウントをけす」クリック時、即時削除ではなく専用POP確認モーダルを表示
  - 対象アカウント名と「すべてのデータ（ポイント・通帳・にがて記録など）が消えて戻りません」の警告を表示
  - 「🗑 はい、けす」「↩ やめる」ボタン、および暗幕（背景）クリックによるキャンセルに対応
  - モーダル表示時のゴミ箱シェイクアニメーションを追加
- **🔍 ブラウザ自動検証の完遂**:
  - カラー選択、絵文字選択、写真切り替え、保存後のアカウントカード反映、削除確認モーダルの開閉・キャンセルを実機確認済

---

## 📁 2. 現在のファイル構成

```
学習アプリ/
├── index.html            # 全画面HTML（アカウント/ポータル/漢字/算数/タイピング/通帳/設定/削除確認/保護者PIN）
├── style.css             # 全スタイル（カラーパレット/アバター写真/削除確認モーダル/学年テーマ/タイピング等）
├── app.js                # 全体ロジック（テーマカラー・絵文字・写真圧縮保存・削除モーダル・通帳・PIN・上限・バックアップ）
├── quiz_data.js          # 漢字問題データ 1〜6年生 全511問
├── math_data.js          # 算数問題自動生成エンジン
├── typing_data.js        # タイピング 3ワールドお題データ＆ローマ字パーサー
├── コンテキスト.md       # 本ドキュメント（次回引き継ぎ用）
├── SPEC_AND_KNOWLEDGE.md # 詳細仕様書＆開発ナレッジ
└── README.md             # プロジェクト概要＆操作マニュアル
```

---

## 🚀 3. 次回実装プラン・今後のTODO

次回セッションで着手可能な推奨機能リストです：

### 🟡 中優先度
1. **GitHub Pages へのデプロイ準備（またはPWA対応）**:
   - iPad Safariで「ホーム画面に追加」した際にネイティブアプリのようにフルスクリーンで快適に起動できるマニフェスト（`manifest.json`）やアイコン設定。
2. **タイピングのワールドC（スイーツ寿司打）の演出強化**:
   - 正解するごとにお皿やパフェの具材が積み重なっていくビジュアルアニメーションの追加。
3. **効果音・BGM（Web Audio API）の追加（ON/OFF設定付き）**:
   - 外部音声ファイル不要の軽量Web Audioシンセ音による打鍵音・正解ファンファーレ・コンボSE。

### 🟢 低優先度（将来キュー）
4. **iPad Apple Pencil / 指での「手書き漢字書き取り」モード**:
   - Canvasを利用した書き順判定・なぞり書きドリル。
5. **週間・月間のがんばりレポートグラフ**:
   - お子様ごとの学習量・正解率の推移をビジュアルチャートで保護者メニューに表示。

---

## 💡 4. 次回作業開始時のクイックスタート

```bash
# 動作確認を行う場合
python3 -m http.server 8080
# ブラウザで http://localhost:8080/index.html を開く
```
